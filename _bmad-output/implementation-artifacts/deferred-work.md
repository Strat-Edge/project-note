# Deferred Work

## Deferred from: code review of story-6.3 (2026-09-03)

- Piège à focus de `DocumentDetail` ne tient pas compte de l'`<iframe>` d'aperçu PDF comme 4e élément focalisable ; limitation fondamentale d'un contenu iframe cross-origin (les événements clavier qu'il reçoit ne remontent jamais au parent) — même classe de lacune déjà acceptée pour `app/capture-flow.tsx` (Story 3.1, signalée par deux revues à l'époque aussi). [app/projects/[id]/project-view.tsx:DocumentDetail]
- `deleteDocument(id)` sur un id sans ligne locale correspondante (référence en mémoire périmée après suppression concurrente) est un no-op inoffensif qui enfile et pousse quand même une suppression pour rien — fenêtre de course étroite, aucune conséquence visible pour l'utilisateur. [data/local/documents.ts:deleteDocument]
- Fenêtre de course étroite : `removeDocumentFile` réussit mais `deleteDocumentRow` échoue ensuite — la ligne Postgres reste avec un `storage_path` pointant vers un fichier déjà disparu ; un téléchargement/aperçu concurrent pendant cette fenêtre reçoit une erreur Storage brute plutôt qu'un message in-app. [sync/server.ts:deleteDocumentAndFile]
- Le lien `<a href>` de téléchargement/l'aperçu n'a aucune gestion in-app d'un 401/404 — le navigateur navigue réellement vers la réponse d'erreur et sort l'utilisateur de la SPA. 401 pratiquement inatteignable (proxy.ts intercepte déjà les sessions expirées) ; 404 nécessite la fenêtre de course ci-dessus. [app/projects/[id]/project-view.tsx (DocumentCard, DocumentDetail)]
- `fetchAllProjectsTasksNotesAndDocuments` n'a aucune pagination — risque de troncature silencieuse à la limite par défaut de PostgREST pour un très grand volume de documents. Caractéristique préexistante de cette fonction (utilisée par tout pull, pas introduite par la Story 6.3), même famille que plusieurs autres risques déjà acceptés dans ce code base pour un outil mono-utilisateur. [data/remote/sync.ts:fetchAllProjectsTasksNotesAndDocuments]
- La réconciliation de suppression cross-appareil (`pullOnce`) purge silencieusement toute entrée de champ en attente pour un document supprimé ailleurs, sans conflit visible — AD-3 cite explicitement "priorité changée sur un appareil, document supprimé sur l'autre" comme son propre exemple à ne jamais silencier. Correspond toutefois à la décision déjà établie et documentée (Story 6.1 Dev Notes AD-3) que `Document.priority` n'est pas conflict-tracké et qu'aucune UI d'édition n'existe — inatteignable aujourd'hui via l'UI. [sync/client.ts:pullOnce (réconciliation)]
- `documentActionError` reste affiché indéfiniment après un échec de suppression tant qu'aucune nouvelle suppression n'est tentée, y compris après changement d'onglet, alors que la modale de confirmation se ferme immédiatement au même moment — léger papillon UX, faible probabilité de déclenchement (écriture Dexie locale pure, échec rare). [app/projects/[id]/project-view.tsx:handleConfirmDeleteDocument]
- Aucun attribut `sandbox` sur l'`<iframe>` d'aperçu PDF — durcissement défense-en-profondeur plausible, mais non vérifiable sans test réel qu'il ne casserait pas le rendu PDF natif du navigateur ; risque déjà très faible (isolation cross-origin déjà effective par nature, app mono-utilisateur AD-9, bucket RLS). [app/projects/[id]/project-view.tsx:DocumentDetail (iframe)]

## Deferred from: code review of story-6.2 (2026-09-02)

- `formatFileSize` (`app/projects/[id]/project-view.tsx`) arrondit toute taille dans la fenêtre ~[1023.5, 1024) Ko à "1024 Ko" au lieu de basculer vers "1,0 Mo" — bug préexistant hérité tel quel de `formatFileSize` (`app/capture-flow.tsx`, Story 6.1), que la Story 6.2 devait explicitement dupliquer à l'identique ("copie conforme"). Corriger uniquement cette copie créerait une incohérence entre les deux fichiers ; un correctif réel nécessiterait de mettre à jour les deux copies ensemble, hors périmètre d'une story de lecture seule.
- `listDocumentsByProject` (`data/local/documents.ts`) effectue un scan `.toArray()` non borné par projet — même forme que `listTasksByProject`/`listNotesByProject` déjà existants. Hypothèse héritée du pattern existant (outil mono-utilisateur à petite échelle), pas introduite par la Story 6.2, mais les documents (fichiers) pourraient s'accumuler plus vite par projet que tâches/notes — à surveiller si le nombre de documents par projet devient significatif en usage réel.

## Deferred from: code review of story-6.1 (2026-09-02)

- Aucune vérification que `documentId` (`app/api/sync/upload-document/route.ts:29`) correspond à un document existant et possédé par l'appelant avant d'accepter l'upload — le chemin est scopé par `userId` (RLS Storage), mais n'importe quelle valeur de `documentId` au format UUID est acceptée telle quelle, pouvant créer un objet Storage orphelin jamais référencé par aucune ligne `documents`. Même pattern déjà présent et jamais corrigé pour `app/api/sync/upload-audio/route.ts` depuis la Story 5.2, pas une nouvelle déviation introduite par cette story.
- Aucune garde de concurrence dans `uploadPendingDocuments` (`sync/client.ts`) — un scan complet `db.documents.toArray()` filtré sur `storagePath === null` à chaque cycle, sans marquage "en cours". Deux cycles de synchro qui se chevauchent (ou deux onglets sur la même IndexedDB) peuvent tenter d'uploader le même document en parallèle ; toléré via `isAlreadyExistsError` côté Storage mais peut produire un double `markDocumentUploaded`/double entrée de file. Même lacune déjà présente pour `uploadPendingAudio` depuis la Story 5.2.
- L'upload du blob (`uploadPendingDocuments`, `sync/client.ts`) peut se terminer avant que la création de la ligne de métadonnées du document elle-même (`enqueueCreate`) n'ait été confirmée synchronisée côté Supabase — `runSyncCycle` appelle `uploadPendingDocuments()` avant `processQueue()`. Un objet Storage peut donc exister brièvement sans ligne `documents` correspondante si le push de métadonnées échoue durablement après un upload réussi. Même ordre déjà en place pour l'audio depuis la Story 5.2, non documenté comme risque à l'époque non plus.
- Le type MIME fourni par le client (`data/remote/document-storage.ts`, `uploadDocumentFile`) est passé tel quel comme `contentType` de l'objet Storage, sans validation contre le contenu réel du fichier. Non exploitable dans cette story (aucun chemin de lecture/téléchargement n'existe encore, cf. Scope boundary), mais Story 6.2/6.3 (qui serviront ces fichiers) ne devraient pas hériter silencieusement de ce risque de type de contenu usurpé sans regarder la question à ce moment-là.

## Deferred from: code review of story-5.2 (2026-09-01)

- Pas de plafond de tentatives sur `uploadPendingAudio` (`sync/client.ts`) — contrairement à `data/local/sync-queue.ts` (`attempts`/`MAX_SYNC_ATTEMPTS`), une note vocale dont l'upload échoue durablement (fichier trop volumineux rejeté par le bucket, mauvaise configuration RLS) réessaie indéfiniment à chaque cycle de synchro, sans distinction entre échec transitoire et permanent. Déjà documenté comme décision de portée volontaire dans les Dev Notes de la Story 5.2 (NFR-5, reprise d'upload explicitement hors périmètre) — pas un oubli silencieux, mais reste une dette réelle si un fichier bloqué apparaît en usage réel.
- Boucle d'upload séquentielle bloquante dans `uploadPendingAudio` (`sync/client.ts`) — chaque note vocale en attente est traitée l'une après l'autre (jusqu'à 60s de timeout chacune) à l'intérieur de la même garde `syncCycleInFlight` que `processQueue()`, pouvant retarder la synchronisation des champs ordinaires (tâches, notes texte) de plusieurs minutes après une longue période hors ligne avec plusieurs notes vocales en attente. Inhérent à la simplicité assumée du design de cette story, aucune AC n'exige un traitement concurrent.
- "Audio en cours de synchronisation." (`app/projects/[id]/project-view.tsx`) s'affiche identiquement pour un upload réellement en cours et pour un enregistrement définitivement perdu (blob local jamais uploadé, appareil d'origine perdu/désinstallé avant la fin de l'upload) — aucun moyen de distinguer les deux cas pour l'utilisateur. Cohérent avec l'indicateur de synchronisation global déjà simple (Story 3.2) ; aucun état d'échec par élément n'existe ailleurs dans l'app non plus.
- Aucun nettoyage du blob local (`data/local/note-audio.ts`) après upload réussi vers Supabase Storage — chaque note vocale duplique indéfiniment son audio entre IndexedDB et Storage, croissance non bornée du stockage local. Cohérent avec l'absence totale de fonctionnalité de suppression de note dans l'app (aucune story ne l'a introduite).
- Aucune vérification que `noteId` (`app/api/sync/upload-audio/route.ts`) correspond à une note vocale existante et possédée par l'appelant avant d'accepter l'upload — le chemin est scopé par `userId` (RLS Storage), mais n'importe quelle valeur de `noteId` est acceptée telle quelle, pouvant créer un objet Storage orphelin jamais référencé par aucune ligne `notes`. Réel en général, mais l'app est architecturalement mono-utilisateur (AD-9, un seul compte, aucun système de permissions/rôles prévu) : aucune exploitation inter-utilisateur n'est possible, tout au plus un objet orphelin auto-infligé par le seul utilisateur du système.
- `toLocalNote` (`sync/client.ts`) caste `row.type` en `Note["type"]` sans validation que la valeur distante vaut bien `"text"` ou `"voice"` — prolonge un pattern déjà établi et déjà consigné en dette (cf. entrée Story 3.2 ci-dessous : `priority`/`provenance`/`status` distants castés sans validation), pas une nouvelle déviation introduite par cette story.
- `NoteDetail` (`app/projects/[id]/project-view.tsx`) peut faire basculer la source de l'élément `<audio>` de l'URL distante (`/api/notes/[id]/audio`) vers le blob local juste après le montage, une fois la lecture IndexedDB résolue de manière asynchrone — fenêtre de quelques millisecondes ; une lecture démarrée dans cette fenêtre est interrompue par le changement de `src`. Cosmétique, jamais rencontré en vérification manuelle.

## Deferred from: code review of story-5.1 (2026-08-19)

- `domain/note.ts` importe `Provenance` depuis `domain/task.ts` — couplage valide au regard d'AD-2 (import interne à `domain/`) mais organisationnellement discutable : `Priority` avait été promu dans `capture.ts` précisément parce que partagé Task/Note/Document, `Provenance` ne l'a jamais été alors qu'il l'est tout autant. Pas un bug, un rangement à revisiter si Document (Epic 6) répète le même contournement.
- `validateNoteContent` (`domain/note.ts:27`) n'impose aucune longueur maximale — un texte de note arbitrairement long est persisté tel quel (Dexie, file de synchro, colonne Postgres `text`). Aucune FR/NFR ne fixe de limite pour le texte libre (NFR-10 ne couvre que documents/audio) ; comportement cohérent avec `validateTaskTitle`/`Task.description`, qui n'ont pas non plus de plafond.
- Aucun indicateur par carte d'un échec de synchronisation persistant sur une note (contrairement au badge de conflit sur `TaskCard`, qui couvre un autre cas). Seul l'indicateur global de synchro signale un échec, sans dire quel élément est concerné — design existant à l'échelle de toute l'app, pas spécifique à Note.
- Les abonnements `liveQuery` (tâches et notes) restent actifs en permanence dans `ProjectView` (`app/projects/[id]/project-view.tsx`), indépendamment de l'onglet affiché, au lieu de s'abonner paresseusement à la sélection de l'onglet. Reproduit à l'identique le pattern déjà en place pour les tâches depuis les Stories 3.3/3.6 ; coût négligeable à l'échelle actuelle de l'app (outil interne solo).
- `createNote` (`data/local/notes.ts:32`) ne valide jamais que `projectId` référence un projet réellement existant avant d'écrire la note et de mettre 6 champs en file — un id invalide échoue silencieusement plus tard en violation de contrainte FK Postgres au push, sans message spécifique à l'utilisateur. Reproduit un manque déjà existant dans `createTask` depuis la Story 3.1.
- Le schéma de la table `notes` et sa policy RLS n'existent nulle part en contrôle de version — uniquement comme bloc SQL ponctuel dans le fichier Markdown de la story 5.1, exécuté à la main par Guillaume. Aucun moyen reproductible de recréer ce schéma (reprise après sinistre, second environnement). Reproduit à l'identique la convention déjà établie par la Story 3.2 pour `projects`/`tasks`.
- Chaque création de note coûte deux aller-retours Postgres (`data/remote/sync.ts`, `upsertNoteFields`/`updateThenUpsert`) : un `UPDATE` qui échoue toujours en silence sur une ligne encore inexistante, puis un `upsert` réel, au lieu d'un `insert` direct pour `operation: "create"`. Inefficacité déjà présente et partagée avec `upsertTaskFields`/`upsertProjectFields`.
- Tous les échecs de synchronisation (`sync/client.ts`, `pullOnce`/`processQueue`) restent des `catch {}` silencieux indifférenciés — la panne réelle rencontrée pendant la vérification de la Story 5.1 (table `notes` absente côté Supabase, 500 sur le pull) aurait été indiagnosticable en production sans inspection réseau manuelle. Pattern déjà en place pour projects/tasks depuis la Story 3.2.
- L'abonnement `liveQuery` des notes (`app/projects/[id]/project-view.tsx:189-199`) n'a aucun chemin de reprise après une émission `error` — un observable Dexie se termine définitivement sur erreur, donc une panne transitoire laisse l'onglet Notes bloqué sur le message d'erreur pour le reste du cycle de vie du composant. Reproduit à l'identique le comportement déjà présent sur l'abonnement `liveQuery` des tâches.

## Deferred from: code review of story-4.3 (2026-08-19)

- Le message d'erreur du nom ne se réinitialise pas pendant la frappe (`app/general-screen.tsx:570`, `ProjectQuickAccess`) — comportement identique à `app/projects/projects-screen.tsx:271`, même gap présent depuis la Story 2.1, non introduit par cette diff.
- Aucune limite de longueur (`maxLength`) sur les champs nom/description de `ProjectQuickAccess` (`app/general-screen.tsx:565-590`) — absent aussi de `app/projects/projects-screen.tsx`, même formulaire source, aucune valeur maximale définie par le PRD/l'Architecture.
- Erreurs de soumission non journalisées (`console.error`) dans `ProjectQuickAccess.handleSubmit` (`app/general-screen.tsx:498-501`) — convention déjà établie par `ProjectsScreen.handleSubmit` et `CaptureFlow.handleSubmitTask`, aucun formulaire du projet ne logge ses échecs.
- Pas de verrouillage du scroll de fond ni d'`inert`/`aria-hidden` sur le contenu sous la modale d'accès rapide (`app/general-screen.tsx:518-527`) — gap identique dans `app/capture-flow.tsx` et `components/confirm-dialog.tsx`, aucune des deux modales existantes ne le fait.
- Couleur de projet potentiellement dupliquée en cas de créations concurrentes via `ProjectQuickAccess` (`app/general-screen.tsx:494`) — `createProject({ name, description, color })` passe toujours `color` explicitement, contournant le fallback atomique `nextProjectColor(existingCount)` de la transaction Dexie ; comportement identique à `ProjectsScreen`, hérité de la Story 2.1.

## Deferred from: code review of story-4.2 (2026-08-18)

- Pattern de case à cocher dupliqué une 3e fois (`app/projects/[id]/project-view.module.css` → `app/general-screen.module.css`, `.visuallyHidden`/`.filter`/`.checkboxInput`/`.checkboxBox`) sans composant partagé — `components/index.ts` n'exporte toujours rien (`export {};`). Convention déjà établie et documentée par la Story 4.2 elle-même (cohérente avec `.viewToggle` dupliqué de `switcher.module.css` en Story 4.1). Candidat sérieux à extraction en composant partagé si un 4e usage apparaît — tout ajustement visuel futur de la case à cocher devra sinon être répété identiquement dans plusieurs fichiers.

## Deferred from: code review of story-4.1 (2026-08-18)

- Décodage du fuseau horaire de `dueDate` : `dateKey`/`groupTasksByDueDate` (`domain/calendar.ts:14-19,36`) décodent l'instant UTC stocké avec le fuseau *local de l'appareil consultant* — un appareil dans un fuseau différent de celui qui a fixé l'échéance peut voir la tâche apparaître au mauvais jour dans le calendrier. Conséquence héritée de la convention `dueDate` déjà en place depuis la Story 3.1 (même hypothèse utilisée par `isTaskOverdue`/`formatDueDate`) ; la Story 4.1 est la première à l'utiliser comme clé de répartition stricte par jour, mais n'introduit pas l'hypothèse elle-même. À revisiter si Guillaume constate un incident réel de fuseau horaire.
- `shiftReferenceDate` (`app/general-screen.tsx:259-266`, navigation mois) ancre toujours au jour 1 avant de changer de mois (évite le bug "31 janvier → 3 mars"). Conséquence : naviguer en vue mois puis basculer en vue semaine ne préserve pas une position comparable dans la semaine. Corrigible mais sans solution évidente sans compromis (préserver le jour du mois réintroduirait le bug de saut de mois que l'ancrage au jour 1 évite). À revisiter si Guillaume signale la surprise en usage réel.
- `listAllTasks()` (`data/local/tasks.ts:102-111`) est un scan complet non filtré, réexécuté par `liveQuery` à chaque écriture sur `db.tasks` (y compris un changement de statut/priorité sans rapport avec le calendrier), sans limite ni pagination. Compromis explicitement documenté et assumé par la Story 4.1 ("compatible avec un scan complet, outil interne solo") — même précédent que la file de synchronisation sans limite de taille (Story 3.2, `sync/client.ts`, déjà consigné ci-dessous). À revisiter si le volume de tâches grandit significativement.
- `today` (mise en évidence de la date du jour, `app/general-screen.tsx:71`) est calculé une seule fois par rendu (`new Date()`), sans minuteur — reste figé sur la veille si le composant reste monté sans autre déclencheur de rendu au passage de minuit. Même limitation déjà acceptée pour `isTaskOverdue` (`domain/task.ts`, Story 3.3+), jamais corrigée par un minuteur dédié dans ce code base.

## Deferred from: code review of story-3.6 (2026-08-13)

- Dérive d'horloge entre appareils (`domain/sync.ts:81`, `resolveFieldSync`) : si l'horodatage d'une écriture locale se retrouve ≤ au point de synchro à cause d'un décalage d'horloge entre les deux appareils, une vraie divergence de valeur peut être classée `"keep-local"` au lieu d'être signalée en conflit. Limitation inhérente à toute résolution de conflit par horodatage — pas propre à la Story 3.6, le reste du code base génère déjà tous ses horodatages côté client sans garantie NTP. À revisiter uniquement si un décalage d'horloge réel entre les appareils de Guillaume cause un incident constaté.
- Aucun commit git depuis la Story 1.1 (`98e3462`) — l'intégralité des Stories 1.2 à 3.6 n'existe qu'en working tree non suivi par git (untracked ou modifié-non-committé). Un `git reset --hard`/`git clean -f` accidentel perdrait tout ce travail sans recours. Constaté à l'activation de la Story 3.6, préexistant, hors périmètre d'un correctif de code. Recommandation : établir une cadence de commit (au minimum un commit par story terminée) avant que le volume de travail non sauvegardé ne grandisse davantage.

## Deferred from: code review of story-3.5 (2026-08-13)

- `selectedTask.isNew` n'est jamais resynchronisé après `markTaskOpened` (`app/projects/[id]/project-view.tsx`, `handleOpenTask`) — seul `tasks` est mis à jour via `.map()`, jamais `selectedTask`. Hérité tel quel de la Story 3.3, non modifié par la Story 3.5. Inatteignable aujourd'hui : `TaskDetail` ne lit jamais `task.isNew`. Deviendrait un vrai bug dès qu'une future story afficherait ce champ dans le détail.
- `TaskDetail` (`app/projects/[id]/project-view.tsx`) n'a ni fermeture par la touche Échap ni clic en dehors du panneau pour fermer — seul le bouton "Fermer" fonctionne. Hérité tel quel de la Story 3.3, non modifié par la Story 3.5. Pattern ARIA APG Dialog standard manquant ; à envisager si un audit accessibilité formel est fait.
- `role="tab"`/`role="tablist"` (`app/projects/[id]/project-view.tsx`) sans navigation flèches Gauche/Droite/Home/End — même constat déjà consigné sous "Deferred from: code review of story-3.3" ci-dessous, reconfirmé ici (non modifié par la Story 3.5).
- `isTaskOverdue` (`domain/task.ts`) n'est réévalué qu'au rendu (`new Date()` au moment du render, appelé depuis `app/projects/[id]/project-view.tsx`), sans minuteur — une tâche affichée peut franchir le seuil "en retard" pendant que l'écran reste ouvert sans que le badge apparaisse avant un prochain rendu déclenché par autre chose. Hérité tel quel de la Story 3.3, non modifié par la Story 3.5.
- `formatDueDate` (`app/projects/[id]/project-view.tsx`) reconvertit l'échéance stockée en UTC vers le fuseau local du navigateur au moment de l'affichage sans fuseau explicite — suppose que le fuseau de l'appareil n'a pas changé depuis la capture. Hérité tel quel de la Story 3.3, non modifié par la Story 3.5. Risque réel pour un utilisateur qui voyage entre la capture et la consultation.
- Géométrie CSS de la coche de case à cocher (`app/projects/[id]/project-view.module.css`, `.checkboxInput:checked + .checkboxBox::after`) en valeurs littérales (`left: 3px; top: 0.5px; width: 4px; height: 7px`) non rattachées par commentaire aux dimensions de `.checkboxBox` (13×13px) — un futur redimensionnement de la case désalignerait silencieusement la coche. Hérité tel quel de la Story 3.4, non modifié par la Story 3.5.

## Deferred from: code review of story-3.3 (2026-08-13)

- Sélecteur d'onglets (`app/projects/[id]/project-view.tsx`, `role="tablist"`/`role="tab"`) sans navigation flèches Gauche/Droite/Home/End — le pattern ARIA APG Tabs cité en Dev Notes comme référence l'inclut normalement, mais ni l'AC ni les Dev Notes de la Story 3.3 n'exigent la conformité complète (seuls les rôles/`aria-selected` sont demandés). À envisager si un audit accessibilité formel est fait.
- `updateThenUpsert` (`data/remote/sync.ts`) suppose qu'un `UPDATE` à 0 ligne affectée signifie toujours "l'entité n'existe pas encore" — vrai tant que `enqueueCreate` garantit un lot complet à la création, mais rien ne fait respecter cet invariant au niveau de l'appelant ; une future mise à jour partielle sur une entité jamais synchronisée réintroduirait le bug NOT NULL que ce correctif (Story 3.3) élimine. Corriger nécessiterait de transporter l'enregistrement complet depuis Dexie à chaque push, hors périmètre de la Story 3.3.

## Deferred from: code review of story-3.2 (2026-08-12)

- Aucune limite de taille/segmentation sur le lot envoyé par `processQueue()` (`sync/client.ts:70`) — après une longue période hors ligne, un seul appel `POST /api/sync/push` porte un tableau non borné. Jugé non prioritaire pour un outil interne solo ; à revisiter si un usage réel avec de longues périodes hors ligne est constaté.
- `deriveState()` (`app/sync-indicator.tsx`) retombe silencieusement sur "À jour" pour les statuts `"synced"`/`"conflict"` — jamais produits par le code de la Story 3.2, mais existeront dès que la Story 3.6 implémentera la résolution de conflit. À traiter dans cette story-là.
- `auth.getClaims()` vérifié uniquement via `if (!data)` dans `app/api/sync/push/route.ts`/`app/api/sync/pull/route.ts`, sans inspecter `error` — pattern déjà établi et utilisé sans modification dans `proxy.ts`/`app/login/page.tsx` depuis les Stories 1.2/1.3. À corriger globalement (tous les call sites à la fois) si jamais revisité.
- `getDeviceId()` (`lib/device.ts`) retourne `"server"` sans garde de type dédiée en environnement serveur — même précédent que `detectProvenance()` dans le même fichier (Story 3.1), risque purement hypothétique (aucun appelant serveur actuel).
- Fenêtre de course étroite et auto-corrective au démarrage de `startSyncEngine()` (`sync/client.ts:150`) : l'écouteur `online` est enregistré avant que `resetStaleSyncingToPending()` ne soit résolue — un événement `online` très précoce peut manquer les entrées orphelines de ce cycle (rattrapées au cycle suivant).
- Les valeurs distantes (`color`/`status`/`priority`/`provenance`, `sync/client.ts:47`) sont castées vers les types locaux sans validation — aucune contrainte `CHECK` sur les colonnes Postgres correspondantes. Risque théorique tant qu'aucune autre voie que cette app n'écrit dans ces tables ; à revisiter si la Story 3.3+ construit de l'UI qui suppose ces valeurs strictement valides.

## Deferred from: code review of story-3.1 (2026-08-11)

- Aucune validation que `reminderAt` précède `dueDate` (`domain/task.ts`, `canSetReminder`) — ne vérifie que la présence de l'échéance, jamais l'ordre chronologique. Non exigé par FR-10/AC#5 de la Story 3.1 ; à envisager si Guillaume constate qu'un rappel programmé après l'échéance de sa propre tâche pose un problème réel.
- Le garde `pathname === LOGIN_PATH` (`components/capture-flow.tsx`) masque `CaptureFlow` sans réinitialiser son état interne (`open`/`pending`/`success`/champs de formulaire) — fenêtre d'atteignabilité quasi nulle aujourd'hui (aucun lien visible vers `/login` une fois authentifié, le middleware redirige déjà les utilisateurs connectés). À revisiter si un scénario de perte de session en cours de capture est un jour rapporté.
- `createTask` (`data/local/tasks.ts`) persiste `projectId` sans vérifier que le projet référencé existe encore ou reste actif, contrairement au pattern `getProjectOrThrow` utilisé par `archiveProject`/`unarchiveProject`. Fenêtre d'exploitation étroite (course multi-onglets : archiver le projet dans un onglet pendant qu'une capture reste ouverte dans un autre), conséquence bénigne (un projet archivé n'est pas supprimé, seulement masqué). À traiter si Story 3.2 (synchronisation) introduit un chemin de résolution plus général pour ce type de référence obsolète.
- Aucun piège à focus (Tab/Maj+Tab) sur l'overlay de capture "+" (`components/capture-flow.tsx`), contrairement à `ConfirmDialog` (Story 2.3) — décision de portée déjà documentée explicitement dans les Dev Notes de la Story 3.1 (effort jugé disproportionné pour un contenu dynamique à 3 étapes), mais signalée indépendamment par deux revues adversariales lors du code review. Un utilisateur clavier peut sortir de la modale ouverte par Tab vers le contenu de la page derrière. À revisiter si un audit accessibilité formel est fait.
- Aucune limite de taille sur les champs Titre/Description d'une tâche (`components/capture-flow.tsx`) — même précédent déjà accepté pour Nom/Description de projet en Story 2.1 (aucune valeur maximale définie par le PRD/l'Architecture pour ces champs). À visiter si Guillaume constate un besoin réel.

## Deferred from: code review of story-2.3 (2026-08-11)

- Le message "L'archivage/désarchivage a échoué. Réessayez." (`app/projects/projects-screen.tsx`) s'affiche aussi pour l'erreur "Projet introuvable." levée par `getProjectOrThrow` (`data/local/projects.ts`), qui est un échec permanent, pas transitoire — actuellement inatteignable depuis l'UI (aucune fonctionnalité de suppression de projet n'existe encore). À revisiter si une suppression de projet est un jour introduite.
- `.projectCard` (`app/projects/projects-screen.module.css`) n'a pas de gestion de débordement (`min-width:0`/`text-overflow`) sur `.projectName` — gap préexistant des Stories 2.1/2.2, désormais un peu plus exposé par le nouveau bouton d'action Archiver/Désarchiver (Story 2.3) qui rivalise pour l'espace avec un nom de projet long. À traiter si un nom de projet réel provoque un débordement visible.
- Aucune annonce `aria-live` lors du passage d'un projet entre les listes actifs/archivés (`app/projects/projects-screen.tsx`) — amélioration d'accessibilité réelle, non explicitement couverte par les exemples du Socle Accessibilité d'`EXPERIENCE.md` (badge "nouveau", statut de synchronisation). À envisager si un audit accessibilité formel est fait.

## Deferred from: code review of story-2.2 (2026-08-11)

- La pastille de couleur présélectionnée à l'ouverture du formulaire de création (`app/projects/projects-screen.tsx`, `openForm`) peut devenir obsolète sous création concurrente (deux onglets/appareils créant un projet presque simultanément) — hérité tel quel de la Story 2.1, non modifié par la Story 2.2. Le fond n'est pas atomique (contrairement à `createProject` lui-même, qui est protégé par une transaction Dexie) : seule la couleur *affichée par défaut* peut être en retard d'une création, la couleur réellement enregistrée reste correcte puisque l'utilisateur peut toujours la changer manuellement avant de valider.
- Les pastilles de couleur du formulaire de création (`app/projects/projects-screen.tsx`) utilisent `aria-pressed` (sémantique bouton bascule) plutôt qu'une sémantique de groupe radio pour un choix mutuellement exclusif — hérité tel quel de la Story 2.1, non modifié par la Story 2.2. Envisager `role="radiogroup"`/`role="radio"` si un audit accessibilité formel est fait.
- `groupProjectsByStatus` (`domain/project.ts`) exclut silencieusement tout projet dont le `status` ne correspond à aucune des deux valeurs connues (`active`/`archived`) — actuellement inatteignable (typage strict, tous les points d'écriture actuels du code garantissent une valeur valide), mais deviendrait un point de perte silencieuse si une future migration de schéma ou une modification manuelle des données introduisait une valeur inattendue. À revisiter si une migration de schéma Dexie est un jour introduite.

## Deferred from: code review of story-2.1 (2026-08-07)

- Indicateur de focus faible sur `.input`/`.textarea` de `app/projects/projects-screen.module.css` (`outline: none` remplacé uniquement par un changement de `border-color`, sans anneau de compensation) — pré-existant, motif identique copié de `login-form.module.css`, en place depuis les Stories 1.2/1.3, pas introduit par la Story 2.1. Concerne toute l'app, pas seulement l'écran Projets — à traiter globalement (ajout d'un `box-shadow`/anneau de focus visible) si un audit accessibilité formel est fait.
- Aucune limite de taille sur les champs Nom/Description d'un projet (`app/projects/projects-screen.tsx`) — aucune valeur maximale n'est définie par le PRD/l'Architecture pour ces champs (NFR-10 ne couvre que la taille des fichiers). Un nom/description démesuré est accepté et stocké tel quel, affiché sans troncature dans la liste. À visiter si Guillaume constate un besoin réel.
- Les noms de projet dupliqués ne sont pas contrôlés (`data/local/projects.ts` `createProject`) — FR-6 n'exige aucune unicité de nom, pas un bug. Si Guillaume souhaite un jour empêcher les doublons, ajouter une vérification dans `createProject` ou à la validation du formulaire.

## Deferred from: code review of story-1.3 (2026-08-07)

- `--color-danger` (`#D64545`) et `--color-muted` (`#7A8594`) échouent au contraste WCAG AA sur fond clair (`#F4F7FB`) — 4.07:1 et 3.48:1 respectivement, sous le seuil 4.5:1 pour texte normal. Valeurs héritées telles quelles de `DESIGN.md`/Strat'Edge, pas une erreur de code. Impact concret : message d'erreur de connexion et libellés de champ moins lisibles que la barre AA. Raison du report : pas prioritaire pour l'instant (Guillaume). À revisiter si Guillaume souhaite ajuster la charte, ou si un audit accessibilité plus formel est fait plus tard.
- `proxy.ts` — le `matcher` en liste d'exclusion codée en dur est un pattern fragile : il a déjà causé un bug dans cette story (route `brand/` oubliée, cassant silencieusement l'optimiseur d'image Next.js pour les visiteurs non authentifiés). Récidivera pour tout futur dossier d'assets `public/` ajouté sans y penser. Envisager une refonte en allowlist de routes protégées plutôt qu'une denylist d'exclusions, quand le nombre de routes publiques grandira.
- Logique de vérification d'authentification (`try { getClaims() } catch { isAuthenticated = false }`) dupliquée à l'identique entre `proxy.ts` et `app/login/page.tsx`. Extraire un helper partagé (`isAuthenticated(supabase)`) quand une deuxième story touchera l'un des deux fichiers, pour éviter une dérive entre les deux copies.

## Deferred from: code review of story-1.1 (2026-08-06)

- `data/local/db.ts` n'est importé nulle part encore ; risque latent si un futur composant serveur importe `data/local` avant qu'un vrai usage client existe. Revisiter dès qu'Epic 2 ajoute le premier usage réel de Dexie (table `Project`) — vérifier à ce moment-là que `db` n'est jamais atteint côté serveur.
- Icônes manifest/apple-touch-icon insuffisantes pour l'installabilité PWA complète (une seule icône SVG `sizes: any`, pas de variante maskable ni PNG 192/512, pas d'apple-touch-icon). À traiter en Story 1.3 (identité visuelle Strat'Edge) avec les vraies icônes.
- Commentaire de `data/remote/client.ts` sur la garde `server-only` : le mécanisme ne bloque que l'inclusion dans le bundle client, pas un import direct par un React Server Component. Corriger le commentaire quand un premier Server Action/route handler consommera réellement ce module.
- Aucune règle ESLint n'impose automatiquement les frontières de dépendance AD-2 (domain/, components/, sync/) — repose entièrement sur la discipline humaine. Envisager `eslint-plugin-boundaries` ou équivalent une fois que ces dossiers contiennent du vrai code.
- Pas de `engines.node`/`.node-version` pour figer la version Node utilisée par Render. Faible risque tant que le déploiement actuel fonctionne, mais à ajouter par prudence.
